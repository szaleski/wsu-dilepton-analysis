
class biasInjector() :
    """
    Inject the curvature bias into the muon pT spectrum
    Compare the positive and negative muon curvature distributions (after normalization)
    and perform the chi-squared test
    """

    def __init__(self, tree, minBias, maxBias, biasStep):
        """Initialize the parameters for the curvature bias injection"""
        self.minBias  = minBias
        self.maxBias  = maxBias
        self.biasStep = biasStep
        
        self.graphErrors = 0


        return
    
    
    def :
        return
